import React from "react";
import ReactDOM from "react-dom";

import StatelessCounter from "./StatelessCounter";

const rootElement = document.getElementById("root");
ReactDOM.render(<StatelessCounter />, rootElement);
